@model EntityLayer.Concrete.Blog

@{
    ViewData["Title"] = "BlogAdd";
    Layout = "~/Views/Shared/WriterLayout.cshtml";
}

<h1>Blog Ekleme</h1>

@using (Html.BeginForm("BlogAdd", "Blog", FormMethod.Post))
{
    @Html.Label("Blog Başlığı")
    @Html.TextBoxFor(x => x.Title, new { @class = "form-control" })
    @Html.ValidationMessageFor(x => x.Title)
    <br />

    @Html.Label("Blog Görseli")
    @Html.TextBoxFor(x => x.Image, new { @class = "form-control" })
    @Html.ValidationMessageFor(x => x.Image)
    <br />

    @Html.Label("Blog Küçük Görseli")
    @Html.TextBoxFor(x => x.ThumbnailImage, new { @class = "form-control" })
    <br />

    @Html.Label("Kategori")
    @Html.DropDownListFor(x => x.CategoryId, new SelectList(ViewBag.cv, "Value", "Text"), "Kategori Seç", new { @class = "form-control" })
    @Html.ValidationMessageFor(x => x.CategoryId)
    <br />

    @Html.Label("Blog İçeriği")
    @Html.TextAreaFor(x => x.Content, 15, 3, new { @class = "form-control" })
    @Html.ValidationMessageFor(x => x.Content)
    <br />

    <button class="btn btn-primary">Kaydet</button>
}
